# ==============================================================================
# AWS Lambda 依存関係（サイズ最適化版）
# ==============================================================================

# Note: boto3/botocore are pre-installed in Lambda runtime

# LangChain framework (v1.x - 2026年2月最新)
langchain>=1.2.9
langchain-core>=1.2.9
langgraph>=1.0.8

# Cloud LLM providers (AWS only)
langchain-aws>=0.3.0                 # AWS Bedrock (Claude Opus 4.x)

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Document processing
pypdf>=3.0.0
openpyxl>=3.1.0
python-docx>=1.0.0

# ==============================================================================
# OCR Providers
# ==============================================================================

# AWS Textract - boto3経由で使用
# 追加パッケージ不要

# Tesseract OCR（Docker版で使用）
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.0              # PDFをTesseractで処理する場合に必要

# Azure Document Intelligence (optional)
# azure-ai-documentintelligence>=1.0.0
# azure-core>=1.30.0

# ==============================================================================
# 非同期処理（オプション）
# ==============================================================================

# DynamoDB/SQS - boto3経由で使用
# 追加パッケージ不要

# ==============================================================================
# Monitoring & Observability（Phase 3: 監視最適化）
# ==============================================================================

# AWS X-Ray統合
aws-xray-sdk>=2.12.0

# OpenTelemetry（オプション）
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
