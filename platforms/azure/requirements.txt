# Azure Functions core
azure-functions>=1.17.0

# LangChain framework (v1.x - 2026年2月最新)
langchain>=1.2.9
langchain-core>=1.2.9
langgraph>=1.0.8                      # LangGraph for workflow orchestration

# Cloud LLM providers
langchain-openai>=0.3.0              # Azure OpenAI Service (GPT-5.x)
langchain-google-vertexai>=3.0.0     # GCP Vertex AI (Gemini 3.x)
langchain-aws>=0.3.0                 # AWS Bedrock (Claude Opus 4.x)

# Utilities
python-dotenv>=1.0.0

# Document processing
pypdf>=3.0.0                          # PDF text extraction
openpyxl>=3.1.0                       # Excel file processing
python-docx>=1.0.0                    # Word file processing

# ==============================================================================
# OCR Providers (選択したプロバイダーのみインストール)
# ==============================================================================

# Azure Document Intelligence (OCR_PROVIDER=AZURE)
azure-ai-documentintelligence>=1.0.0  # Document Intelligence SDK
azure-core>=1.30.0                    # Azure core for credentials

# Azure Storage (非同期ジョブ管理用)
azure-data-tables>=12.4.0             # Table Storage for job persistence
azure-storage-queue>=12.8.0           # Queue Storage for job notifications
azure-storage-blob>=12.19.0           # Blob Storage for large evidence files

# AWS Textract (OCR_PROVIDER=AWS) - boto3はlangchain-awsで既に含まれる
# boto3>=1.34.0                       # langchain-awsに含まれるため不要

# GCP Document AI (OCR_PROVIDER=GCP)
# google-cloud-documentai>=2.0.0      # 使用時にインストール

# Tesseract OCR (OCR_PROVIDER=TESSERACT) - ローカルOSS
# pytesseract>=0.3.10                 # 使用時にインストール
# pdf2image>=1.16.0                   # PDF→画像変換（Tesseract用）
# Pillow>=10.0.0                      # 画像処理（Tesseract用）

# ==============================================================================
# Monitoring & Observability（Phase 3: 監視最適化）
# ==============================================================================

# OpenTelemetry - 分散トレース基盤
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# Azure Application Insights統合
azure-monitor-opentelemetry>=1.0.0
